# 📦 راهنمای ساخت و نصب نسخه دسکتاپ چاپینو (Windows 11)

با دنبال‌کردن این راهنما یک فایل **Setup.exe** واقعی می‌سازید که برنامه را مثل هر نرم‌افزار ویندوزی نصب می‌کند: با ویزارد نصب، انتخاب مسیر، **میان‌بر روی دسکتاپ** و منوی استارت. پس از نصب، کاربر نهایی به Node.js نیاز ندارد.

---

## گام ۱ — پیش‌نیازها (فقط روی سیستم سازنده)

| ابزار | نسخه | لینک |
|---|---|---|
| Node.js | 20 LTS یا جدیدتر | nodejs.org |
| PostgreSQL | 15 یا جدیدتر | postgresql.org/download/windows |

⚠️ هنگام نصب PostgreSQL رمزی که برای کاربر `postgres` می‌گذارید را یادداشت کنید.

---

## گام ۲ — آماده‌سازی پایگاه‌داده (یک بار)

1. پوشه پروژه را روی ویندوز کپی کنید.
2. روی فایل زیر دابل‌کلیک کنید:

```
desktop\db-setup.bat
```

این اسکریپت:
- دیتابیس `app_db` را می‌سازد
- فایل `.env` را ایجاد می‌کند (اگر رمز شما `postgres` نیست، داخل `.env` اصلاحش کنید)
- جداول برنامه را با `drizzle-kit push` می‌سازد

---

## گام ۳ — ساخت فایل Setup.exe (یک دابل‌کلیک)

روی فایل زیر دابل‌کلیک کنید:

```
desktop\build-setup.bat
```

این اسکریپت به‌ترتیب: وابستگی‌ها را نصب، برنامه را build، و با **electron-builder** فایل نصبی NSIS را می‌سازد.

خروجی نهایی:

```
desktop\dist-setup\Chapino-Setup-1.0.0.exe
```

---

## گام ۴ — نصب برنامه

فایل `Chapino-Setup-1.0.0.exe` را اجرا کنید:

- ویزارد نصب فارسی/انگلیسی باز می‌شود
- مسیر نصب قابل انتخاب است
- ✅ میان‌بر **دسکتاپ** و **منوی استارت** با نام «چاپینو» ساخته می‌شود

از این پس برنامه با دابل‌کلیک روی آیکون دسکتاپ باز می‌شود؛ پنجره اختصاصی خودش را دارد (بدون مرورگر) و سرور را داخل خودش اجرا می‌کند.

---

## نکات مهم

1. **PostgreSQL باید روشن باشد.** سرویس PostgreSQL به‌صورت پیش‌فرض با ویندوز بالا می‌آید؛ اگر برنامه پیام خطای اتصال داد، از `services.msc` سرویس `postgresql-x64-…` را Start کنید.
2. **انتقال به سیستم دیگر:** روی سیستم مقصد فقط PostgreSQL را نصب کنید، `db-setup.bat` را یک بار اجرا کنید، سپس Setup.exe را نصب کنید.
3. **پشتیبان‌گیری دیتابیس:** با دستور زیر یک فایل پشتیبان کامل بگیرید:
   ```
   "C:\Program Files\PostgreSQL\16\bin\pg_dump" -U postgres app_db > backup.sql
   ```
4. **تغییر نسخه:** برای نسخه جدید فقط `build-setup.bat` را دوباره اجرا کنید؛ نصب روی نسخه قبلی به‌روزرسانی می‌شود و داده‌ها (که در PostgreSQL هستند) دست نمی‌خورند.

---

## ساختار فایل‌های دسکتاپ

| فایل | نقش |
|---|---|
| `desktop/main.js` | پوسته Electron: اجرای سرور داخلی + پنجره برنامه |
| `desktop/package.json` | تنظیمات electron-builder و نصاب NSIS |
| `desktop/icon.png` | آیکون برنامه و میان‌بر |
| `desktop/db-setup.bat` | آماده‌سازی یک‌باره دیتابیس |
| `desktop/build-setup.bat` | ساخت خودکار فایل Setup.exe |
